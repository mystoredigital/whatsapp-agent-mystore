services:
  app:
    build: .
    container_name: whatsapp-agent-mystore
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./credentials:/app/credentials
