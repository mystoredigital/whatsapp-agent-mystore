services:
  whisper:
    # ASR sidecar — API OpenAI-compatible para transcribir voice notes inbound.
    # CPU build, modelo small (multi-idioma, buen español). El cache vive en
    # ../files/whisper-cache para que la imagen no se redescargue al redeploy.
    image: fedirz/faster-whisper-server:latest-cpu
    container_name: whatsapp-agent-whisper
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - ../files/whisper-cache:/root/.cache/huggingface
    environment:
      WHISPER__MODEL: Systran/faster-whisper-small
      WHISPER__INFERENCE_DEVICE: cpu
      WHISPER__COMPUTE_TYPE: int8
      DEFAULT_LANGUAGE: es

  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: whatsapp-agent-mystore:latest
    container_name: whatsapp-agent-mystore
    restart: unless-stopped
    depends_on:
      - whisper
    networks:
      - dokploy-network
    volumes:
      - ../files/data:/app/data
      - ../files/credentials:/app/credentials
    environment:
      PORT: ${PORT}
      DASHBOARD_USER: ${DASHBOARD_USER}
      DASHBOARD_PASS: ${DASHBOARD_PASS}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL}
      OPENROUTER_SITE_URL: ${OPENROUTER_SITE_URL}
      OPENROUTER_SITE_NAME: ${OPENROUTER_SITE_NAME}
      GOOGLE_SERVICE_ACCOUNT_FILE: ${GOOGLE_SERVICE_ACCOUNT_FILE}
      GOOGLE_CALENDAR_ID: ${GOOGLE_CALENDAR_ID}
      TIMEZONE: ${TIMEZONE}
      BUSINESS_NAME: ${BUSINESS_NAME}
      BUSINESS_DESCRIPTION: ${BUSINESS_DESCRIPTION}
      GHL_CLIENT_ID: ${GHL_CLIENT_ID}
      GHL_CLIENT_SECRET: ${GHL_CLIENT_SECRET}
      GHL_SHARED_SECRET: ${GHL_SHARED_SECRET}
      GHL_CONVERSATION_PROVIDER_ID: ${GHL_CONVERSATION_PROVIDER_ID}
      GHL_REDIRECT_URI: ${GHL_REDIRECT_URI}
      GHL_WEBHOOK_PUBLIC_KEY: ${GHL_WEBHOOK_PUBLIC_KEY}
      # Anti-ban / patrón humano
      HUMAN_DELAY_MIN_MS: ${HUMAN_DELAY_MIN_MS}
      HUMAN_DELAY_MAX_MS: ${HUMAN_DELAY_MAX_MS}
      PER_CHAT_COOLDOWN_MS: ${PER_CHAT_COOLDOWN_MS}
      GLOBAL_MAX_PER_MIN: ${GLOBAL_MAX_PER_MIN}
      QUIET_HOURS_START: ${QUIET_HOURS_START}
      QUIET_HOURS_END: ${QUIET_HOURS_END}
      NEW_CONTACT_REQUIRES_HUMAN: ${NEW_CONTACT_REQUIRES_HUMAN}
      # R2 media storage
      R2_ACCOUNT_ID: ${R2_ACCOUNT_ID}
      R2_BUCKET: ${R2_BUCKET}
      R2_ENDPOINT: ${R2_ENDPOINT}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
      R2_PUBLIC_URL: ${R2_PUBLIC_URL}
      # Whisper local
      WHISPER_URL: ${WHISPER_URL}
      WHISPER_MODEL: ${WHISPER_MODEL}
      WHISPER_LANGUAGE: ${WHISPER_LANGUAGE}
      # Flujo odoopago -> webhook n8n
      N8N_ODOOPAGO_WEBHOOK_URL: ${N8N_ODOOPAGO_WEBHOOK_URL}
      N8N_ODOOPAGO_TIMEOUT_MS: ${N8N_ODOOPAGO_TIMEOUT_MS}
    labels:
      - traefik.enable=true
      - traefik.http.routers.whatsapp-agent-mystore.entrypoints=websecure
      - traefik.http.routers.whatsapp-agent-mystore.rule=Host(`wa.mystoredigital.cloud`)
      - traefik.http.routers.whatsapp-agent-mystore.tls.certresolver=letsencrypt
      - traefik.http.services.whatsapp-agent-mystore.loadbalancer.server.port=3000

networks:
  dokploy-network:
    external: true
